apiVersion: v1
kind: Secret
metadata:
  name: dee-metrics-token
  labels:
    app.kubernetes.io/name: meiro-dee
    app.kubernetes.io/component: observability
type: Opaque
stringData:
  # Use a DB-backed token with viewer or admin scope.
  token: replace-with-viewer-token
